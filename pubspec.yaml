name: flutter_calory_calculator
description: Калькулятор калорий

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get:
  flutter_svg: ^1.1.6
  supabase_flutter: ^1.2.0
  gradient_borders: ^0.2.0
  intl: 
  collection: 
  expandable: ^5.0.1
  syncfusion_flutter_charts: ^21.1.39
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets: 
    - assets/images/